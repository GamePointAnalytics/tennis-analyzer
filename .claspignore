# Ignore everything by default
**/**

# Allow manifest
!appsscript.json

# Allow backend source code
# NOTE: .claspignore patterns are evaluated relative to rootDir (backend/src),
# so match the bare filenames there, not the backend/src/ prefix.
!*.js
!*.gs
